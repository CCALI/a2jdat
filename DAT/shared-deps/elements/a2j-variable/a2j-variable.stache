<can-import from="caja/shared-deps/elements/a2j-variable/a2j-variable.less!" />

{{#if(canShowAnswer)}}
  <span class="var-val">{{value}}</span>
{{else}}
  {{#if(name)}}
    <span class="var-name{{#if(isUnanswered)}} unanswered{{/if}}">{{name}}</span>
  {{/if}}
{{/if}}
